a=[1,2,3,4,5]
k=2
def solution(a,k):
    origLenth = len(a)
    leftOver = k % origLenth
    if(k<2*len(a))
        newArrayLen = origLenth + k
    newArray = [0]* newArrayLen
    
    return newArrayLen
